<h1>Listing directory_employee_units</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @directory_employee_units.each do |directory_employee_unit| %>
  <tr>
    <td><%= link_to 'Show', directory_employee_unit %></td>
    <td><%= link_to 'Edit', edit_directory_employee_unit_path(directory_employee_unit) %></td>
    <td><%= link_to 'Destroy', directory_employee_unit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee unit', new_directory_employee_unit_path %>
