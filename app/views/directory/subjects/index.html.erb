<h1>Listing directory_subjects</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @directory_subjects.each do |directory_subject| %>
  <tr>
    <td><%= link_to 'Show', directory_subject %></td>
    <td><%= link_to 'Edit', edit_directory_subject_path(directory_subject) %></td>
    <td><%= link_to 'Destroy', directory_subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subject', new_directory_subject_path %>
