<% directory_menu %>
<% subject_row = SubjectRow.new(@subject) %>

<% breadcrumb([link_to('Employee Directory', directory_root_path), link_to('Subject Areas', directory_subjects_path), subject_row.name ]) %>

<% content_for :content_title do %>
  <div class="page-header">
      <h1>
        <%= subject_row.full_name %>

        <% if @permission.current_user_can_edit?(subject_row) %>
          <div class="pull-right small">
            <%= link_to "Edit Subject", directory_admin_subject_path(@subject), :class => "btn btn-primary" %>
          </div>
        <% end %>

      </h1>
  </div>
<% end %>


<div class="orgUnitShow">

  <!--<div id="members">
    <div class="highlight"><h2>Subject Librarian Specialist / Laison</h2></div>
    <div class="subsection">

      <dl class="dl-horizontal">
        <% subject_row.subject_librarians.each do |sl| %>

          <dt><%= link_to sl.display_name, directory_employee_path(sl) %></dt>
          <dd>
            <% sl.all_titles.each do |slt| %>
              <%= "#{slt}<br /> ".html_safe %>
            <% end %>

          </dd>

        <% end %>
      </dl>

    </div>
  </div>-->
  <div id="members">
    <div class="highlight"><h2>Subject Librarian Specialist / Laison</h2></div>
    <div class="subsection">

      <% subject_row.subject_librarians.each do |e| %>
      <% emp = EmployeeShowView.new(e, @permission) %>

      <div class="name"><%= link_to emp.first_last, directory_employee_path(emp.netid) %></div>
      <hr />

      <div class="staffInfo pull-left">
        <% emp.all_titles.each do |eut| %>
        <div class="title"><%= eut %></div>
        <% end %>


        <div class="contact">

          <address>
            <% if emp.primary_phone %>
            <%= emp.primary_phone %> |
            <% end %>

            <%= mail_to(emp.primary_email) %>

            <% emp.addresses.each do |a| %>
            | <%= a %>
            <% end %>

          </address>

        </div>

        <% if emp.about_text %>
        <div class="aboutTextParagraph">
          <%= emp.about_text_paragraph(0) %>

          <% if emp.about_text_paragraph(1) %>
          <a href="javascript:void(0)" class="btn-link" data-toggle="collapse" data-target="#hiddenAboutText_<%= emp.id %>">- more -</a><br />
          <% end %>

        </div>
        <% end %>
      </div>

      <div class="staffImage pull-right">
        <% if !emp.photo_url.nil? %>
        <%= image_tag(emp.photo_url, width: "80%", class: "pull-right") %>
        <% end %>
      </div>

      <div class="clearfix"></div>
      <% if emp.about_text_paragraph(1) %>
      <div id="hiddenAboutText_<%= emp.id %>" class="aboutText collapse in"><%= emp.about_text_paragraph(1) %></div>
      <% end %>

      <br />
      <% end %>


    </div>
  </div>
  <% if subject_row.webpages.count > 0 %>
    <div id="contact">
      <div class="highlight"><h2>Webpage Links</h2></div>
      <div class="subsection">

        <% subject_row.webpages.each do |w| %>
          <%= link_to w.contact_information, w.contact_information %>
        <% end %>

      </div>
    </div>
  <% end %>
</div>


