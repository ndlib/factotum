<%= order.title %>
==========================

<% just_say_yes_auto_fields.each do |field|
  if field.is_a?(Array)
    caption = field[0]
    field = field[1]
  else
    caption = field.to_s.humanize
  end 
  if order.send(field).present? %>
<%= caption %>: <%= order.send(field) %>
<% end 
end %>

<% if order.additional_details.present? %>
Additional Details:
<%= order.additional_details %>
<% end %>

Submitted by: <%= order.creator.to_s %> on <%= order.created_at.strftime("%m/%d/%Y at %I:%M%p") %>