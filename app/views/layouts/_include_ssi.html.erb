<% cache("ssi/#{active_branch_subdomain}/#{filepath}", :expires_in => 1.day) do %>
  <!-- <%= "#{active_branch_subdomain}/#{filepath} cache: #{Time.now.to_s}" %> -->
  <%= raw read_ssi_file(filepath) %>
<% end %>
