<h1>Buildings</h1>

<p>
  Select a building to view floor maps
</p>

<table class="table table-striped table-condensed">
 <thead>
 <tr>
   <th>Name</th>
 </tr>
 </thead>
 <tbody>
  <% @buildings.each do | building | %>
  <tr>
    <td><%= link_to h(building.name), maps_building_floor_maps_path(building) %></td>
  </tr> 
  <% end %>
</tbody>
</table>