# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  no_current_hours_error_message: There are no currently active hours for %{service_name}.  Please create a new hours record or edit an existing hours so that it is published over the current date. We will fall back on the most recent regular hours in the meantime.
  gap_in_between_regular_hours: The end date of the previous hours, %{end_date} is not continuous with the start date of the next hours, %{start_date}. Edit the hours so that there is no gap between the published dates.
  gap_in_regular_hours: There is a gap in between regular hours for %{service_name}.  Edit the hours so that there is no gap between the published dates.
  time:
    formats:
      default: "%m/%d/%Y %I:%M%p"
      us: "%-m/%-d/%Y"
      day_month: "%-d %B"
  date:
    formats:
      default: "%m/%d/%Y"
      day_month: "%-d %B"
  time:
    formats:
      us: "%-m/%-d/%Y"
      day_month: "%-d %B"
  activerecord:
    attributes:
      purchase_request:
        requester_name: "Name"
        requester_phone: "Phone"
        requester_netid: "NetID"
        requester_email: "Email"
        requester_department: "UND Department"
        requester_affiliation: "Affiliation"
        requester_notification_preference: "Notification Preference"
